pipeline {
    agent any
    stages {
        stage('Production') {
            steps {
                sh 'pwd'
                sh 'ls -ltr'
                sh 'docker-compose -f docker-compose-prod.yml up -d'
                sh 'docker ps'
            }    
        }
    }
}
